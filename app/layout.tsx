import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"
import { ClerkClientProvider } from "@/components/clerk-provider"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "DSCD.app - Discord Shortlinks",
  description: "Create memorable shortlinks for Discord servers and profiles. Share dscd.sh and dscd.me links.",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className="font-sans antialiased">
        <ClerkClientProvider>{children}</ClerkClientProvider>
        <Analytics />
      </body>
    </html>
  )
}
